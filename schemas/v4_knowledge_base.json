{
  "bsonType": "object",
  "title": "v4.1 Knowledge Base (RAG Source)",
  "required": ["tenantId", "domainId", "question", "vector_embedding"],
  "properties": {
    "tenantId": {
      "bsonType": "string",
      "description": "Data Silo Owner"
    },
    "domainId": {
      "bsonType": "string",
      "description": "Logic Cluster (sales vs support)"
    },
    "question": {
      "bsonType": "string",
      "description": "Grounded user query"
    },
    "answer": {
      "bsonType": "string",
      "description": "Deterministic factual response"
    },
    "tags": {
      "bsonType": "array",
      "items": { "bsonType": "string" }
    },
    "vector_embedding": {
      "bsonType": "array",
      "items": { "bsonType": "double" },
      "description": "1536-dim vector for similarity search"
    }
  }
}